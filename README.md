# 💬 Chatty-App: A Real-Time Chat Application

A real-time chat application built with the MERN stack (MongoDB, Express, React, Node.js), featuring user authentication, image uploads with Cloudinary, and instant messaging with Socket.io.

## 🛠️ Tech Stack

**Backend**: Node.js, Express, MongoDB, Socket.io, JWT, Cloudinary
**Frontend**: React.js

## 🚀 Getting Started

1.  Clone the repository and run `npm install` in both `/backend` and `/frontend`.
2.  Create a `backend/.env` file with your MongoDB, JWT, and Cloudinary credentials.
3.  Start the backend: `cd backend && npm run dev`.
4.  Start the frontend: `cd frontend && npm start`.
